# Chart generated by NVFlare `provision && provision -p project.yml`
# with some minor modifications to allow for providing additional values
persist: /tmp # /home/nvflare
workspace: /tmp # /home/nvflare

# Custom values added outside of what is provided by `provision -p project.yml`
# overseer:
#   image:
#     repository: xxx
#     tag: yyy

# server:
#   image:
#     repository: xxx
#     tag: yyy
